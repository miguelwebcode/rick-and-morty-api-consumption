<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Web app that consumes the Rick and Morty API</h1>
    <% if(locals.content) {%>


        <form id="myForm" method="get">

            <input id="charactersInput" type="submit" value="<%=content.characters.name %>" formaction="/characters" />
            <input id="locationsInput" type="submit" value="<%=content.locations.name %>" formaction="/locations" />
            <input id="episodesInput" type="submit" value="<%=content.episodes.name %>" formaction="/episodes" />
          </form>
    <% } else { %>
    <p>Not receiving content</p>
    <% } %>

</body>
</html>