<%- include('partials/header.ejs') %> <%if(locals.character){%>
<div class="characterDetailBody">
  <div class="detailContainer">
    <h1><%=locals.character.name%></h1>
    <img
      class="characterDetailImg"
      src="<%=locals.character.image%>"
      alt="<%=locals.character.name%>"
    />
    <div class="detailData">
      <div class="propertyContainer">
        <p class="propertyParagraph">Status:</p>
        <form id="myForm" method="post">
          <input
            class="linkButton"
            type="submit"
            value="<%=`${character.status[0].toUpperCase()}${character.status
              .slice(1)}`%>"
            formaction="/search"
          />
          <input type="hidden" name="searchProperty" value="status" />
          <input
            type="hidden"
            name="searchValue"
            value="<%=`${character.status[0].toUpperCase()}${character.status
              .slice(1)}`%>"
          />
          <input type="hidden" name="endPoint" value="character" />
        </form>
      </div>
      <div class="propertyContainer">
        <p class="propertyParagraph">Species:</p>
        <form id="myForm" method="post">
          <input
            class="linkButton"
            type="submit"
            value="<%=`${character.species[0].toUpperCase()}${character.species
              .slice(1)}`%>"
            formaction="/search"
          />
          <input type="hidden" name="searchProperty" value="species" />
          <input
            type="hidden"
            name="searchValue"
            value="<%=`${character.species[0].toUpperCase()}${character.species
              .slice(1)}`%>"
          />
          <input type="hidden" name="endPoint" value="character" />
        </form>
      </div>
      <div class="propertyContainer">
        <p class="propertyParagraph">Gender:</p>
        <form id="myForm" method="post">
          <input
            class="linkButton"
            type="submit"
            value="<%=`${character.gender[0].toUpperCase()}${character.gender
              .slice(1)}`%>"
            formaction="/search"
          />
          <input type="hidden" name="searchProperty" value="gender" />
          <input
            type="hidden"
            name="searchValue"
            value="<%=`${character.gender[0].toUpperCase()}${character.gender
              .slice(1)}`%>"
          />
          <input type="hidden" name="endPoint" value="character" />
        </form>
      </div>
      <div class="propertyContainer">
        <p class="propertyParagraph">Origin:</p>
        <% if(character.origin.name === "unknown") { %>
        <p class="unknownData">Unknown</p>
        <% } else {%>
        <form id="myForm" method="post">
          <input
            class="linkButton"
            type="submit"
            value="<%=character.origin.name%>"
            formaction="/getLocationById"
          />
          <input
            type="hidden"
            name="locationId"
            value="<%=character.origin.id%>"
          />
          <input type="hidden" name="endPoint" value="character" />
        </form>
        <%}%>
      </div>
      <div class="propertyContainer">
        <p class="propertyParagraph">Location:</p>
        <% if(character.location.name === "unknown") { %>
        <p class="unknownData">Unknown</p>
        <% } else {%>
        <form id="myForm" method="post">
          <input
            class="linkButton"
            type="submit"
            value="<%=character.location.name%>"
            formaction="/getLocationById"
          />
          <input
            type="hidden"
            name="locationId"
            value="<%=character.location.id%>"
          />
          <input type="hidden" name="endPoint" value="character" />
        </form>
        <%}%>
      </div>
    </div>
  </div>
</div>
<p class="pageSubtitle">Episodes:</p>
<ul class="noPointsUl">
  <div class="episodesContainer">
    <%locals.character.episode.forEach(episode => {%>
    <li>
      <form id="myForm" method="post">
        <input
          class="darkButton"
          type="submit"
          value="<%= episode.id%>: <%=episode.name%>"
          formaction="/getEpisodeById"
        />
        <input type="hidden" name="episodeId" value="<%=episode.id%>" />
      </form>
    </li>
    <%});%>
  </div>
</ul>
<%} else {%>
<p class="noContent">Not receiving content</p>
<%}%> <%- include('partials/footer.ejs') %>
